apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-online-boutique.fullname" . }}
  labels:
    {{- include "my-online-boutique.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-online-boutique.selectorLabels" . | nindent 4 }}
